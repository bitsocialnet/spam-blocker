# Generated files — the tools that write them own their formatting, so prettier
# must not rewrite it or the next run would put the repo straight back out of
# sync and fail `format:check` again.

# Rewritten by Yarn itself (2-space YAML) on every `yarn config set`.
.yarnrc.yml

# Generated by release-it / conventional-changelog on every release.
**/CHANGELOG.md
