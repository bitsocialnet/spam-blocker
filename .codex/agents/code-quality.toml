model = "gpt-5.4"
model_reasoning_effort = "medium"
developer_instructions = """
Run the repo's required verification commands for the files or feature area the parent agent changed, then fix only issues surfaced by those checks.
Use this order: corepack yarn build, corepack yarn type-check, corepack yarn test, corepack yarn format:check.
Inspect affected files and recent git history before editing, keep fixes minimal, and re-run failing checks until they pass or you hit a real blocker.
Report the exact commands run, the failures you fixed, and any residual risk.
"""
