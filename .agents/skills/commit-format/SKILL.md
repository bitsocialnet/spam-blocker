---
name: commit-format
description: Format a Conventional Commit message when a commit or wording suggestion is requested.
---

# Commit Format

Use `type(scope): short description`, with a concrete scope and an outcome-focused title. Add a body only for context a reviewer needs. Provide wording when requested; creating a commit still follows the user’s authorization.
